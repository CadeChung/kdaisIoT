<!DOCTYPE html>
<html lang="en">

<head>
    <title>註冊帳號</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <!-- 引用boostrap v5.1.3 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" 
          rel="stylesheet">
    <!-- Google 字型 -->
    <link rel="stylesheet"
          href="https://fonts.googleapis.com/css2?family=Noto+Sans+TC&display=swap" >
    <!-- 讀取本地端css檔案 -->
    <link rel="stylesheet"
          href="/css/register.css?<%= new Date().getTime() %>">
</head>

<body>
    <div class="container">
        <div class="d-flex justify-content-center h-100">
            <div class="card">
                <div class="card-header text-center">
                    <h3>註冊帳號</h3>
                </div>
                <div class="card-body">
                    <form action="/register" method="POST">

                        <div class="form-floating mb-3">
                            <input type="text"
                                   class="form-control"
                                   id="floatingInput"
                                   name="userName"
                                   placeholder="userName"
                                   required pattern="[0-9A-z]{4,12}">
                            <label for="floatingInput">請輸入使用者帳號：</label>
                            <div id="usernameHelp" class="rounded-3 form-text text-center text-dark">需符合4-12位字母數字</div>
                        </div>

                        <div class="form-floating mb-3">
                            <input type="email" 
                                   class="form-control" 
                                   id="floatingInput" 
                                   name="email"
                                   require placeholder="email">
                            <label for="floatingInput">請輸入Email</label>
                            <div id="emailHelp" class="rounded-3 form-text text-center text-dark">請輸入Email格式</div>
                        </div>
        
                        <div class="form-floating mb-3">
                            <input type="password" 
                                   class="form-control"
                                   id="floatingPassword"
                                   name="password"
                                   placeholder="password"
                                   require pattern="[0-9A-z]{8,12}">

                            <label for="floatingPassword">請輸入密碼：</label>
                            <div id="passwordHelp" class="rounded-3 form-text text-center text-dark">需符合8-12位字母數字</div>
                        </div>

                        <div class="form-floating mb-3">
                            <input type="password"
                                   class="form-control" 
                                   id="floatingPassword" 
                                   name="second_password"
                                   placeholder="second_Password"
                                   required pattern="[0-9A-z]{8,12}">

                            <label for="floatingPassword">請輸入第二次密碼：</label>
                            <div id="passwordHelp" class="rounded-3 form-text text-center text-dark">需符合8-12位字母數字</div>
                        </div>

                        <div class="d-grid mt-3">
                            <button type="submit" class="btn btn-primary">註冊帳號</button>
                        </div>
                    </form>
                </div>

                <div class="card-footer">
                    <div class="d-flex justify-content-center links text-dark">
                        已經有帳號了?<a href="login">返回登入</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>