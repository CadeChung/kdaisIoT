<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- 引用boostrap v5.1.3 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" 
          rel="stylesheet">
    <!-- Google 字型 -->
    <link rel="stylesheet"
          href="https://fonts.googleapis.com/css2?family=Noto+Sans+TC&display=swap" >
    <!-- 讀取本地端css檔案 -->
    <link rel="stylesheet"
          href="/css/login.css?<%= new Date().getTime() %>">
    <!-- 讀取本地端jquery檔案 -->
    <script src ="/js/jquery-3.6.4.min.js"></script>
    <script src = "/js/login.js"></script>
    <title>文心蘭物聯網場域</title>
</head>
<body>
    <div class="container">
        <div class="d-flex justify-content-center h-100">
            <div class="card">

                <div class="card-header text-center">
                    <h3>文心蘭應用場域</h3>
                </div>

                <div class="card-body">
                    <!-- 加入警告訊息的div元素 -->
                    <div id="login-message"
                        class="alert alert-danger"><%= message %>
                    </div>
                    
                    <form id="login-container" action="/login" method="POST">

                        <div class="form-floating mb-3">
                            <input type="text"
                                   class="form-control"
                                   name = "username"
                                   required placeholder="userName"
                                   pattern="[0-9A-z]{4,12}">
                            <label for="floatingInput">
                                請輸入使用者帳號:
                            </label>
                            <div id="usernameHelp" class="rounded-3 form-text text-center text-dark">
                                需符合4-12位字母數字
                            </div>
                        </div>
                        
                        <div class="form-floating mb-3">
                            <input type="password" 
                                   class="form-control"
                                   name ="password" 
                                   required placeholder="Password"
                                   pattern="[0-9A-z]{8,12}">
                            <label for="floatingPassword">請輸入密碼：</label>
                            <div id="passwordHelp" class="rounded-3 form-text text-center text-dark">需符合8-12位字母數字</div>
                        </div>

                        <div class="d-grid mt-3">
                            <button type="submit" class="btn btn-primary">
                                登入
                            </button>
                        </div>
                    </form>
                </div>

                <div class="card-footer">
                    <div class="d-flex justify-content-center links text-dark">
                        還沒有帳號?<a href="register">註冊帳號</a>
                    </div>
                    <div class="d-flex justify-content-center text-dark">
                        <a href="forgotpassword">忘記密碼?</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>