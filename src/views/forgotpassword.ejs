<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>忘記密碼</title>
    <!-- 引用boostrap v5.1.3 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" 
          rel="stylesheet">
    <!-- Google 字型 -->
    <link rel="stylesheet"
          href="https://fonts.googleapis.com/css2?family=Noto+Sans+TC&display=swap" >
    <!-- 讀取本地端css檔案 -->
    <link rel="stylesheet"
          href="/css/forgotpassword.css?<%= new Date().getTime() %>">
</head>
<body>
    <div class="container">
        <div class="d-flex justify-content-center h-100">
            <div class="card">
                <div class="card-header text-center">
                    <h3>重新設定密碼</h3>
                </div>
                <div class="card-body px-5">

                    <!-- 告警訊息 -->
                    <div>
                        <% if(errors != "") { %>
                            <% errors.forEach((item) => { %>
                                <div class="auth-alert">
                                    <%- item %>
                                </div>
                            <% }); %>
                        <% } %>
                    </div>

                    <div>
                        <% if (locals.isSent) { %>
                            <div class="auth-alert-success">重置密碼郵件已成功發送，
                            <br>請檢查您的電子郵件。</div>
                        <% } %>
                    </div>

                    <p class="card-text py-2 text-center">
                        請輸入您的電子郵件地址，我們將發送一封電子郵件，並重新設定您的密碼。
                    </p>
                    <form action="/forgotpassword" method="POST">
                        <div class="form-floating form-outline">
                            <input type="email" 
                                   id="typeEmail" 
                                   name = "email"
                                   class="form-control my-3" 
                                   placeholder="Enter email" require/>
                            <label class="form-label" for="floatingInput">輸入Email</label>
                        </div>
    
                        <div class="d-grid mt-4">
                            <button type="submit" class="btn btn-primary w-100">重新設定密碼</button>
                        </div>
    
                        <div class="d-flex justify-content-between mt-4">
                            <a class="" href="login">返回登入介面</a>
                            <a class="" href="register">註冊帳號</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>